// Placeholder `File` for JSON round-trip when `source` is exported as "LocalFile" (no real file handle in JSON).
export const importedLocalFilePlaceholder = new File([], "imported-placeholder.txt", {
  type: "text/plain",
});
